# torch is not pinned here — use the version pre-installed by your environment
# (Colab provides torch with CUDA support, pip installing it can break things)
transformers>=4.38.0
peft>=0.8.0
datasets>=2.16.0,<3.0.0
trl>=0.8.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
scikit-learn>=1.3.0
pyyaml>=6.0
wandb>=0.16.0
